# 2026-09-12

---

## 【夜间作业】通读项目 + 仓库两端同步体系建立（2026-09-12 00:10–01:40）

用户睡觉前交代：只做与板子无关的事，尽量多推进；完整读 Ubuntu 侧项目日志与重要文件，
在 Windows 工作区形成完整上文；把 GitHub 仓库纳入工作区，两端每次作业同步。

### 1. 仓库同步体系（已完成并验证）

**仓库**：<https://github.com/SteveGuo1726/coralnpu-gesture>（**public**）

| 项 | 事实 |
|---|---|
| Ubuntu 端 | `/home/steveguo/coralnpu-gesture`，61 GB 但 **git 只跟踪 1168 个文件**（其余是 .gitignore 掉的构建产物） |
| Ubuntu git 布局 | `.git` 是 **gitfile** → 真目录在 `.git-local`（separate-git-dir）；另有空的 `.git.broken` |
| Windows 端 | `C:\Users\SteveGuo\Documents\coralnpu-gesture`，已 `git init` + fetch + checkout 为同一仓库的克隆 |
| 凭据 | Ubuntu/WSL 的 **SSH 密钥可用**（`Hi SteveGuo1726!`）；Windows 端**无密钥无 credential helper** |
| 因此 | remote 故意分开：`fetch = HTTPS`（两端都能拉）、`push = SSH`（由 Ubuntu/WSL 推） |

**同步工具**：仓库根 `sync_repo.sh`，子命令 `status / ubuntu-push / win-pull / win-to-ubuntu`。

### 2. ⚠️ 本机 agent 环境的 git 陷阱（重要，写进 sync_repo.sh 头部）

1. **git 的 ref 锁写入被拦**：`git fetch` 报 `* [new branch] main -> origin/main`，
   但 `.git/refs/remotes/` 下**没有文件**、`git rev-parse origin/main` 说 "not a commit"。
   手动往 `.git/refs/...` 写文件是**正常**的 ⇒ 是 lockfile+rename 流程被拦。
   **后果**：连续两次 commit 会让本地分支**静默分叉**。
   **对策**：每次操作后显式 `git fetch && git rev-parse refs/remotes/origin/main` 确认。
2. **"提交成功"不可信**：出现过 `git commit` 报成功、但提交里**没有**实际改动
   （`--stat` 只有别的文件）。**对策**：提交后 `git show --stat HEAD`，
   并从远端 `git show refs/remotes/origin/main:<path>` 回读关键文件确认。
3. **分叉修复姿势**（保留工作树里真正想要的改动）：
   `cp <文件> /tmp/keep` → `git fetch` → `git reset --hard refs/remotes/origin/main`
   → `cp /tmp/keep <原路径>` → `git add` → `commit` → `push`。
   **不要 merge/rebase**，直接以远端为基线重做更干净。
4. **`git reset --soft` 会连带把工作树里的旧文件差异一起提交**（曾因此误回退了
   `sync_repo.sh` 的 43 行改进，后来专门补了一个提交修复）。
5. **永远只有一端 commit** —— Windows 端无推送能力，以 Ubuntu 为提交端。

### 3. 发现并修复的真实问题

- **`main.c` 的 AArch64 修复一直没进仓库**：先前那个提交（`40e1daa`）的提交信息写了
  这个修复，但 `--stat` 显示只含 `sync_repo.sh`。经查工作树里有修复、HEAD 与远端都没有。
  已用 `53378db` 真正提交，并从远端回读确认 `main.c:570` 有 `#if defined(__aarch64__)`。
- **本地/远端分叉**：本地有 `ca52b23`、远端有 `d165e76/40e1daa/f8c1687`（内容重复）。
  按上述姿势修复，现在是线性的 `a0fa5fd`。

### 4. 仓库现状（远端 main = `a0fa5fd`）

```
a0fa5fd docs: add the project's architectural hard gates to the context document
27ddaf8 docs: add project's architectural hard gates ...（后来被 a0fa5fd 修正）
a0fa5fd ... ↑ 实际最终
d7f4f4d chore(sync): document the ref-write and divergence pitfalls
53378db fix(zcu104): guard the ARMv7-only abort exception IDs for AArch64
f8c1687 docs: handoff 145 - ZCU104 platform bring-up (36.96 FPS) and PetaLinux pitfalls
d165e76 docs: add a self-contained project context document for the Windows workspace
40e1daa fix(zcu104): ...（实际只含 sync_repo.sh）
acf515e feat(zcu104): PetaLinux bring-up tooling, ZCU104 port fixes and session docs
```

### 5. 通读项目后的产出

**新增文档**：`gesture_project/docs/项目完整上下文_Windows工作区版_2026-09-12.md`（713 行，16 章）
—— 自包含，只读这一份就能理解项目全貌。内容涵盖：项目定位与铁律、实板成果与频率演进、
逐层 PL cycle 画像、算法与蒸馏、硬件架构与模块表、**DMP 双乘打包完整推导**、
软件侧事实、已确认的错误判断清单、**项目六条架构硬门禁**、**权重驻留纠错**、
7020→ZCU104 迁移现状与实测数据、摄像头路线、仓库同步规则、文档地图、命令速查、术语表。

**新增交接记录**：`会话交接` 文档顶部插入 **145 号**（条数说明同步改为 145），
按项目约定了记录本轮 ZCU104 打通与 PetaLinux 五个坑。

**本轮读过的关键文档**（8 份）：README、工程文件索引、会话交接 144–139 号、
仓库共享版、从零入门完全指南、DMP 双乘打包、ZCU104 JTAG 首次检测、
ZCU104 上板前审查、ZCU104 推进计划、权重驻留的架构真相、7020 帧率优化方案。

### 6. 项目侧两个最有价值的认识（供后续决策）

1. **六条架构硬门禁**（`7020帧率优化完整方案…§1.4`）：权重生命周期、控制边界、
   CoralNPU 微架构对齐、数据流计账、连续帧证据、创新性界定。
   其中"**只报 MAC/cycle 而不报数据搬运与 PS 开销，不构成性能结论**"直接适用于
   我们 ZCU104 上测到的 36.96 FPS（PL 17.02ms + CPU 10.04ms）。
2. **跨帧权重驻留在当前硬件上不可能**（32KB SRAM vs 200KB 权重，差 6.25 倍），
   指纹驻留是空转。**第一优先动作是把权重装载从 AXI-Lite 逐字写换成 PL DMA burst**
   （实测 114ms → 预期 1~5ms）。这条对 ZCU104 同样适用且收益更大。

### 7. 构建状态（板子无关，仍在跑）

`petalinux-build` 已完成 setscene（1865 个）并进入真实编译：
`gcc-cross-aarch64`、`binutils-cross-aarch64`、`linux-xlnx-udev-rules`、
`kernel-module-vcu` 都在编译中。`images/linux/` 已有
`zynqmp_fsbl.elf / pmufw.elf / bl31.elf / u-boot.elf / system.dtb / boot.scr`，
**日志 ERROR 数 = 0**。缺 `system.bit / image.ub / rootfs.ext4`。
已挂守候任务，完成后会汇报。

---

## 【第 4 次构建失败】DWC3 host-only 链接失败 —— 还是我配错了（00:33）

`petalinux-build` 在 `linux-xlnx do_compile` 失败：

```
aarch64-xilinx-linux-ld.bfd: drivers/usb/dwc3/core.o: in function `dwc3_remove':
core.c:(.text+0x580): undefined reference to `dwc3_gadget_exit_hibernation'
core.c:(.text+0x86c): undefined reference to `dwc3_gadget_exit_hibernation'
make[1]: *** [scripts/Makefile.vmlinux:34: vmlinux] Error 1
```

### 根因（源码级确证）
`kernel_uvc.cfg` 里我写了：
```
CONFIG_USB_DWC3_DUAL_ROLE=n
CONFIG_USB_DWC3_HOST=y
```
生成的 `.config` 确认：
```
CONFIG_USB_DWC3_HOST=y
# CONFIG_USB_DWC3_GADGET is not set
# CONFIG_USB_DWC3_DUAL_ROLE is not set
```

而 Xilinx 内核（6.1.30）的 `core.c` **无条件**调用 gadget 专有函数：
- `drivers/usb/dwc3/core.c:2104`（`dwc3_remove` 内）
- `drivers/usb/dwc3/core.c:2399`（`dwc3_suspend` 内）
- 两处都在 `#ifdef CONFIG_PM_SLEEP` 内，**没有** `CONFIG_USB_DWC3_GADGET` 守卫

定义在 **`drivers/usb/dwc3/gadget_hibernation.c:399`**（gadget 专有文件），声明在 `gadget.h:139`。
host-only ⇒ 该文件不参与编译 ⇒ 符号未定义 ⇒ vmlinux 链接失败。

`drivers/usb/dwc3/Kconfig` 的 `USB_DWC3_HOST` help 原文就写着
*"Select this when you want to use DWC3 in host mode only, thereby the gadget feature will be regressed."*
—— 这个模式本身就会退化 gadget 特性。而 Kconfig 的 mode choice 默认值：
```
default USB_DWC3_DUAL_ROLE if (USB && USB_GADGET)
```
Xilinx defconfig 只写 `CONFIG_USB_DWC3=y`（即默认 DUAL_ROLE=y），**并不用 host-only**。

### 正确做法
**DUAL_ROLE + 设备树 `dr_mode="host"`**。host 行为由设备树决定，不是由 Kconfig 模式决定。
ZCU104 板级 DTS（`{BOARD zcu104-revc}`）已经有 `dr_mode = "host"`，所以 dual-role 驱动照样把控制器跑成 host。

### 修正
`kernel_uvc.cfg` 改为：
```
CONFIG_USB_DWC3=y
CONFIG_USB_DWC3_DUAL_ROLE=y
```
（去掉 `CONFIG_USB_DWC3_HOST=y`、`CONFIG_USB_DWC3_DUAL_ROLE=n`；去掉多余的 `CONFIG_USB_XHCI_PCI=n` override）
原理写进 fragment 头部注释防回退。

**验证**：重建后 `.config` 变为
```
# CONFIG_USB_DWC3_HOST is not set
CONFIG_USB_DWC3_DUAL_ROLE=y
```
已提交推送 `5966654`。

### 教训（第 3 次同类失误）
这已经是**第三次**因为"按直觉写配置、没先查源码/归属"而失败：
1. `PARALLEL_MAKE` 填 `-j8`（应为纯数字，生成逻辑自己加 `-j `）
2. `package-management` 当包启用（实为 Image Feature）
3. `CONFIG_USB_DWC3_HOST=y`（应保持 DUAL_ROLE）

共同模式：**PetaLinux/Kconfig 的某些符号语义与字面直觉不符**。以后动任何 Kconfig/配置符号前，
先做两件事：① `grep` 生成逻辑或源码里的消费点；② 确认它是 recipe / image feature / kernel config 中的哪一类。

### 另注
`/mnt/e/zcu104_vivado/plnx/` 是 E 盘暂存副本，工作区仓库与 Ubuntu 仓库各有一份
`gesture_project/innovation_npu/board_zcu104/plnx/`。**编辑时必须明确以哪份为准**，
本轮就因为在 E 盘改、却从工作区拷贝，导致第一次同步把旧内容又拷回去了。
正确做法：以 E 盘为源 → 同时拷到工作区仓库 + Ubuntu 仓库 + PetaLinux 工程消费点，再校验 md5。

---

## 【PetaLinux 构建成功 + Linux 侧 NPU/摄像头链路落地】（00:30–01:00）

### 一、PetaLinux 构建最终成功
经 4 次失败（全部是我自己配错 + 一个环境问题），最终 `BUILD_RC=0`：

```
Image 22.9 MB | image.ub 9.97 MB | rootfs.ext4 429 MB | system.dtb | system.bit
zynqmp_fsbl.elf | pmufw.elf | bl31.elf | u-boot.elf | boot.scr
BOOT.BIN 21 MB（petalinux-package --boot 另生成）
```

四次败因：

| # | 症状 | 真实原因 | 归属 |
|---|---|---|---|
| 1 | `Can't get compiler version from gcc --version output` | 缺 `en_US.UTF-8`；`rdiArgs.sh:37` 硬编码 `LC_ALL=en_US.UTF-8`，缺 locale 时 `/bin/sh` 往 stderr 吐 setlocale 警告，BitBake 把 stderr 并入 stdout 且**只看第一行**做正则 | 环境（用户跑 sudo 修） |
| 2 | `ValueError: invalid literal for int() with base 10: '-j8'` | `CONFIG_YOCTO_PARALLEL_MAKE` 要**纯数字**；`gen_plnx_machine.py` 自己拼 `PARALLEL_MAKE = "-j %s"` | **我配错** |
| 3 | `Nothing RPROVIDES 'package-management'` | 它是 **Image Feature** 不是 recipe；Vitis 教程清单把两类混在一起 | **我配错** |
| 4 | `undefined reference to dwc3_gadget_exit_hibernation` | 我把 DWC3 改成 host-only；Xilinx 内核 `core.c:2104/2399` **无守卫**调用 gadget 专有函数（定义在 `gadget_hibernation.c`） | **我配错** |

**共同模式（第 2/3/4 次）**：照直觉/照抄教程写配置，没先查生成逻辑或符号归属。
以后动任何 Kconfig 符号前：① grep 生成/消费点；② 确认它是 recipe / image feature / kernel config 哪一类。

### 二、ZCU104 原生路线拿到铁证
反编译 `images/linux/system.dtb`：
```dts
/* zcu104-revc.dtsi:458 —— 随 MACHINE_NAME=zcu104-revc 自动引入 */
&dwc3_0 { dr_mode = "host"; snps,usb3_lpm_capable; maximum-speed = "super-speed"; };
&usb0   { phys = <&psgtr 2 PHY_TYPE_USB3 0 2>; };   /* PS-GTR Lane2, ref clk2 */
```
**USB3 Host 不是我们补的，是官方板级 DTSI 白送的。** 这一条同时解释了为什么 host-only 是错的方向。

### 三、设备树新增 reserved-memory（V0 校验通过）
`plnx/system-user.dtsi` → `reserved-memory { npu-buffers@70000000 { reg = <0x0 0x70000000 0x0 0x1000000>; no-map; } }`

`no-map` 是关键：不加就还是 System RAM，`devmem_is_allowed()` 会拦掉；加了则被 `memblock_remove` 摘出 RAM 映射 → `/dev/mem` 可 map 且内核不会占用。

同时把 `CONFIG_SUBSYSTEM_EXTRA_BOOTARGS` 设为 `"rw"`（开发期方便），生成结果 `... root=/dev/mmcblk0p2 ro rootwait rw`（末尾 rw 生效）。

**V0 校验全过**：reserved-memory 在 / bootargs 含 `clk_ignore_unused` 且末尾 rw / `dr_mode="host"` 回归无损 / memory 节点 `0x0–0x7FF00000` 无冲突 / rootfs 含 `v4l-utils 1.23.0`(v4l2-ctl) + `usbutils 014`(lsusb) + `libv4l`。

### 四、已实测：这些东西**本来就对**，不要"修"
读构建产出的 `.config` 与 dtb：

```
CONFIG_DEVMEM=y                       ← 已经开着
# CONFIG_STRICT_DEVMEM is not set      ← 比预期还宽松
CONFIG_UIO_PDRV_GENIRQ=m              ← 回退方案可用
clk_ignore_unused                     ← 已在 bootargs 里（PetaLinux 默认）
root=/dev/mmcblk0p2                   ← SD 启动已是默认
```

**重要经验**：kernel config fragment 是**按内容校验和**的，**连只加注释都会触发 25 分钟内核重编**。
所以这些"无需改动"的结论写进 `plnx/PITFALLS.md`，**不写进 `kernel_uvc.cfg`**。

### 五、Linux 用户态 NPU 驱动 + 摄像头链路（新交付）
`board_zcu104/linux/`：
- `gf_npu.h/.c` —— 裸机驱动的用户态移植。寄存器协议、权重灌入、21 tile 调度、逐层校验全部照搬；只换访存方式（`/dev/mem` mmap）与计时源。
- `gf_npu_probe.c` —— 移植验证 CLI（参考图自检 + benchmark）
- `gf_camera.c` —— V4L2 → RGB → 面积平均缩放到 96×96 → 送 NPU → 类别 + 多数投票平滑
- `Makefile` —— SDK 或显式 `CROSS_COMPILE` 交叉编译

**两个必须记牢的正确性细节（都对着 RTL 核实过，不是猜的）**：

1. **PL 自己做的 −128 重定心**。`rtl/gestureflow_hp0_rgb_loader.sv:58-60`：
   `pixel_rgb = {~byte[7], byte[6:0]}` 即 XOR 0x80，按 int8 解释就是 `u − 128`。
   ⇒ **驱动只写原始 uint8 像素，绝不能自己减 128**。这一条如果搞错，全流程结果全错。
2. **预处理链**：`train_static_cnn.py:1041` 是 `Image.open(p).convert("RGB").resize((96,96), Image.BILINEAR)`
   —— 直接拉伸、不裁剪、不保长宽比；且 PIL 的 BILINEAR 缩小是**抗锯齿**的。
   ⇒ 驱动用**面积平均**（box）近似，比朴素双线性/最近邻都更贴近。

**访存方案**：寄存器 `0xA0000000` + 缓冲区 `0x70000000`（16 MB）都走 `/dev/mem`，两段都是非缓存
⇒ **整条 HP0 非相干链路上不需要任何 cache flush/invalidate**。这同时针对裸机版 10.04 ms CPU 开销的一个可能来源。

**V0b 校验通过**：用 WSL 本机 gcc 做真实编译+链接，三个 .c 无错误无警告，两个二进制无未定义符号
（204 KB / 209 KB，未用的 golden 数组被 -O1 丢弃）。

### 六、Yocto recipe 让二进制直接进 rootfs
`yocto/gf-npu/gf-npu_1.0.bb`：把 18 个权重头文件从 `board_7020/software/` 原样拷入 `files/`（保持量化模型单一真源），
`DEPENDS = "libjpeg-turbo"`（poky 里有 2.1.5.1）开 MJPEG 支持，`-DGF_HAVE_JPEG`。
注册进 `user-rootfsconfig` + `CONFIG_gf-npu=y` → 板上 `/usr/bin/gf_npu_probe`、`/usr/bin/gf_camera`，无需 SDK。

### 七、SD 卡脚本重写
`plnx/04_make_sd.sh` 补了三处：显式 root 权限检查、**写完 `resize2fs` 撑满分区**（否则 429 MB 之外全废）、
写后重新挂载校验（并拒绝烧录 manifest 里没有 gf-npu 的 rootfs）。

### 八、诚实清单：仍未证明的事
- V1–V6 全部未上板验证（板子关机）
- 摄像头具体支持哪些格式/分辨率未知，需 `gf_camera --list` 实测
- `/dev/mem` 是否真被拦、PL 时钟是否真被关，只有上板才知道
- 中间 6 个阶段的 FNV 期望值**尚无权威基线**（裸机版只校验 conv0/pool1/GAP-FC 三处）
  → 用户态版本额外用软件算并打印 conv0/pool1/conv2/pool2/conv4/pool3/head1x1 的 FNV，留下基线供日后二进制对比

### 九、提交
`8da4f64`（10 文件 / 2585 行）已推送到 GitHub，含设计文档、驱动、recipe、PITFALLS.md。

---

## 【校验强度升级 + 上板操作卡】（01:00–01:15）

### 一、发现：各层 golden 张量都在头文件里导出了
把每个导出数组算一遍 FNV1A，与头文件里的 FNV 宏对照，**实证**确定了 golden 与阶段的对应关系
（不是靠名字猜）：

| 阶段 | 存储张量 | golden 数组 | golden FNV1A | 对应宏 |
|---|---|---|---|---|
| conv0 | 147456 | `gf_body2_layer_input` | `05307466` | `GF_FULL_OUTPUT_FNV1A` ✓ |
| pool1 | 36864 | `gf_pool_output` | **`33B1A130`** | `GF_POOL_OUTPUT_FNV1A` |
| conv2 | 73728 | `gf_conv2b_layer_input` | `90D6DF2F` | `GF_CONV2A_OUTPUT_FNV1A` ✓ |
| pool2 | 18432 | `gf_pool2_output` | **`8439E9C9`** | `GF_POOL2_OUTPUT_FNV1A` |
| conv4 | 27648 | `gf_conv3b_layer_input` | `591D321E` | `GF_CONV3A_OUTPUT_FNV1A` ✓ |
| pool3 | 6912 | `gf_pool3_output` | **`D10D7766`** | `GF_POOL3_OUTPUT_FNV1A` |
| GAP(64) / FC(18) | — | `gf_post_gap_golden` / `gf_post_fc_golden` | `F109A7AE` / `22E52DDD` | 对应宏 ✓ |

### 二、顺带纠正一个易误读点（重要）
**融合了池化的层，硬件 `GF_OUTPUT_FNV1A` 报的不是存进 DDR 的池化张量。**
conv1 跑完后它读 `GF_BODY2_OUTPUT_FNV1A = GF_POOL_INPUT_FNV1A = 0x7E276C7B`，
即**池化前**卷积输出的 FNV；而 `gf_pool_output` 是 `0x33B1A130`。
⇒ 裸机版那处检查实际在查 MAC 输出流，**池化结果从未被验证过**。

### 三、升级：中间 6 个阶段改为字节级 memcmp
原来只查 DMA 写入字节数 → 现在逐张量 `memcmp` 对 golden。
实现在 `gf_npu.c` 的 `verify_tensor()`：

- **只在 `stats != NULL` 时执行**（`gf_npu_probe`）。实时摄像头循环传 NULL 走快速路径 ——
  否则每帧对非缓存内存做 ~0.6 MB 的 memcmp+FNV1A 会**污染 V4 要测的 CPU 侧开销**。
- 校验耗时单独累计进 `cpu_checks_ms`，报告里**从 CPU 开销中扣除**，保证与裸机 10.04 ms 同口径。
- 失败时打印**首个差异字节偏移 + 两侧值 + 两侧 FNV**，必须可定位。
- 唯一缺 golden 的是 head1x1 的 12×12×64 输出，只留软件 FNV 基线。
- 副作用：二进制 205 KB → 516 KB（golden 数组终于被引用，链接器不再丢弃）。

### 四、另一个发现
`gestureflow_chain_body_data.h` 与 `gestureflow_real_conv4x4_body2_layer.h`
**共用同一个 include guard `GESTUREFLOW_REAL_CONV4X4_BODY2_LAYER_H`**，
所以先被包含的那个赢，后者的 `gf_full_layer_input` 在正常 include 顺序下**永远取不到**。
两者内容是同一个 147456 字节张量。这个坑不影响正确性，但会让人困惑。

### 五、已通过的静态门禁（累计）
- **V0** 设备树：reserved-memory + bootargs 末尾 rw + `dr_mode="host"` 回归无损
- **V0b** 本机 gcc 编译+链接：三个 .c 无错误无警告，两个二进制无未定义符号（含升级后）
- **V0c** 从 `rootfs.ext4` 用 `debugfs` 取出二进制：`ARM aarch64`、NEEDED 库齐全

### 六、产出
- `docs/ZCU104_上板操作卡_2026-09-12.md` —— **早上照着做即可的单页操作卡**：
  镜像清单、SD 卡命令、SW6 极性（ON=0/OFF=1）、串口参数、板上四步（含判据）、
  故障排查表（症状→原因→动作）、按收益排序的后续优化。
- 技能 `~/.workbuddy/skills/petalinux-wsl-build/SKILL.md` —— 把本轮 PetaLinux 的
  全部坑（locale / PARALLEL_MAKE / package-management / DWC3 / MACHINE_NAME /
  fragment 校验和 / reserved-memory+dev-mem 模式 / Yocto recipe 模式 / 排错顺序）固化。
- 提交：`fe94327`（content 校验升级）、`5522911`（146 记录修正）、`897c239`（上板操作卡）。

### 七、SDK 构建
`petalinux-build --sdk` 跑了 17 分钟无产物，为**优先让 content 校验进镜像**主动终止。
之后可重启（sstate 已缓存，会快些）。**它不是关键路径** —— recipe 模式下改代码只需
`petalinux-build` 重编 gf-npu（~1 min）+ 重打 rootfs 与镜像（~5 min）。

---

## 【交叉核对与仓库卫生检查】（01:15–01:20）

### 一、类名表逐项核对通过
`models/hagrid_v1_500k_384p_targethand_student_4x4_rvv_distill_20260827/labels.txt` 共 18 项：
```
call, dislike, fist, four, like, mute, ok, one, palm, peace,
peace_inverted, rock, stop, stop_inverted, three, three2, two_up, two_up_inverted
```
与 `gf_npu.c` 里的 `GF_CLASS_NAMES[18]` **顺序完全一致** ✓
⇒ 板上打印的类名不会错位。这条值得核对，因为类名顺序错了会表现为"识别率很低"而不是报错。

### 二、主线模型指标（从 keras_eval.json / tflite_eval.json 读到）
| 项 | 值 |
|---|---|
| 测试样本 | 85311 |
| Keras 精度 | **0.9888056639823704** |
| INT8 TFLite 精度 | **0.9887822203467314** |
| 量化损失 | 仅 0.0024 pct |
| 输入 | `(null, 96, 96, 3)` |
| 模型体积 | `model_int8.tflite` 109552 B（107 KB） |

### 三、21 个 tile 的参数逐项对照（人工复核）
把 `gf_npu.c` 的每次 `exec_conv_tile()` 调用与裸机版逐个对照，全部一致：
- `mode`：0 / 1 / 1 / 2 / 2 / 3 / 5，GAP-FC 用 4
- `store_control`：1（不池化）/ 3（融合池化）交替，与裸机一致
- `stride_bytes`：16 / 16 / 32 / 32 / 48 / 48 / 64 —— 等于该层输出通道数
- `output_lanes`：16 / 16 / GF_CONV2A/2B_OUTPUT_LANES / GF_CONV3A/3B_OUTPUT_LANES / 64
- `groups`：1 / 2 / 2 / 4 / 4 / 6 / 6
- `tile_lanes`：全部 16；tile 数 1/1/2/2/3/3/4
- `valid_bytes`（launch_layer 末参）：恒为 16

### 四、仓库卫生检查
- **`.venv` 未被跟踪**（0 个文件），且已在 `.gitignore`（第 32 行）✓ —— 不是问题。
- `innovation_npu/backup_original_2026-09-01/` 有 **302 个被跟踪文件**，是 `board_7020/`(129)
  与 `tests/`(160) 的完整快照。占仓库约 15 MB 重复内容。
  **这是用户有意保留的基线备份，不动**；仅记录。
- `.git-local` 37 MB，跟踪文件 1180 个 —— 体积健康。
- 跟踪文件按目录：backup 302 / board_validation 254 / tests 160 / board_7020 129 /
  learning·RVV·hdl 112 / rtl 44 / board_zcu104 31。
  ⇒ `board_zcu104/` 已从 5 个文件增长到 31 个，本轮贡献了 20 个。

### 五、本轮最终提交序列
```
e796672  perf(zcu104): stop zeroing the tensors every frame, and bound-check the arena
897c239  docs: add a ZCU104 on-board runbook
5522911  docs(handoff): correct 146's verification section to the stronger implementation
fe94327  feat(zcu104): verify every stage bit-exactly against the exported golden tensors
2679601  docs(context): refresh the workspace context doc for the Linux-side state
5724e74  docs: handoff 146
8da4f64  feat(zcu104): Linux userspace NPU driver, USB3 camera pipeline, DT scratch region
5966654  fix(zcu104): keep DWC3 in dual-role
```
