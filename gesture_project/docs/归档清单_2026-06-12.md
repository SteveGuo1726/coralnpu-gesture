# 归档清单 2026-06-12

## 1. 归档目的

这次归档不是为了“删除历史”，而是为了同时完成两件事：

1. 把大量已经过时、碎片化、短期内不会再直接使用的材料移出活跃区，降低后续查阅成本。
2. 保留完整备份和清晰映射，确保任何旧试验、旧结论、旧配置都还能回查。

备份根目录：

- `/home/steveguo/beifen/coralnpu-gesture`

## 2. 活跃区保留原则

活跃区只保留下面三类材料：

### 2.1 总路线与阶段总览

- `gesture_project/docs/项目计划.md`
- `gesture_project/docs/项目全程阶段总结_阶段1_算法与数据集探索.md`
- `gesture_project/docs/项目全程阶段总结_阶段2_official回放与current_best收敛.md`
- `gesture_project/docs/项目全程阶段总结_阶段3_rowhandoff硬件参考线与真实trace闭环.md`
- `gesture_project/docs/项目全程阶段总结_阶段4_当前状态与后续执行路线.md`
- `gesture_project/docs/新对话对接_当前唯一有效入口_2026-06-12.md`
- `gesture_project/docs/工程文件索引.md`
- `gesture_project/docs/新对话续接说明.md`

### 2.2 当前仍直接服务主线的说明

- current best 主线
- rowhandoff mode=1 语义
- 官方 CSR 接入
- 板级最小验证
- Bazel/output_base 规范
- 计划书重审文档

### 2.3 当前仍直接服务主线的结果

- 正式模型结果
- official current best 回放
- tail / row-end / official patch 量化
- rowhandoff trace / CSR / sideband / reconstruction 闭环

## 3. 本次待归档范围

本次整理前统计结果：

- `docs` 共 `85` 份
- `reports` 共 `603` 份
- `notes` 共 `15` 份

当前建议归档数量：

- `docs` 归档 `67` 份
- `reports` 归档 `515` 份
- `notes` 归档 `15` 份

活跃区保留：

- `docs` 保留 `18` 份旧有主线文档，外加本轮新增总览入口文档
- `reports` 保留 `88` 份主线直接相关结果

## 4. 归档分桶

### 4.1 `docs_archive/阶段1_算法与早期路线探索`

适合归档的内容：

- 早期算法路线讨论
- 候选模型说明
- 训练推进细节
- 已确认失败的结构尝试说明

由以下新文档接管：

- `gesture_project/docs/项目全程阶段总结_阶段1_算法与数据集探索.md`

### 4.2 `docs_archive/阶段2_current_best收敛前的中间控制探索`

适合归档的内容：

- `conv2_3x3_b` 4x8x8 控制器说明
- 共骨架控制器草案
- 早期 handshake / tile / FSM 细节

由以下新文档接管：

- `gesture_project/docs/项目全程阶段总结_阶段2_official回放与current_best收敛.md`

### 4.3 `docs_archive/阶段2_48x48微patch与第二层试验碎片`

适合归档的内容：

- `flat64`
- `x4helper`
- `tail_closure`
- `rowbase_recur` 多 mode 对比
- `post_right_edge`
- `trace_gate` 等逐次试验说明

由以下新文档接管：

- `gesture_project/docs/项目全程阶段总结_阶段2_official回放与current_best收敛.md`
- `gesture_project/docs/项目全程阶段总结_阶段3_rowhandoff硬件参考线与真实trace闭环.md`

### 4.4 `reports_archive/阶段1_算法与模型评估原始结果`

适合归档的内容：

- 早期 digits smoke
- 早期静态候选原始预测 CSV
- 早期 MobileNet/StaticCNN 原始估算
- 不再作为当前主入口的中间结果

### 4.5 `reports_archive/阶段2_策略比较与中间回放`

适合归档的内容：

- strategy1/2/3/4/5/6/7 对比
- refined/proxy/gated/interiorx2/interiorx4* 中间比较
- 不再是当前正式 best 的中间 totals compare

### 4.6 `reports_archive/阶段2_控制器与RTL草案中间产物`

适合归档的内容：

- handshake/fsm/transition/spec/register table 中间结果
- controller prototype / pseudo sv 中间版本

### 4.7 `reports_archive/阶段3_第二层微试验原始回放`

适合归档的内容：

- rowhandoff 多 mode 原始 replay
- tail closure 多 trial 原始 replay
- currentbest rerun 对账碎片
- 多组 elf diff 中间件

由活跃区保留的内容只剩：

- 已经形成正式结论的 rowhandoff source/trace/csr/board 合同
- 真实 workload trace 解析与重建

### 4.8 `notes_archive/原始排障与工作笔记`

`notes/` 整体适合迁入备份区，原因是：

- 其中大部分已经被新阶段总结接管
- 它们保留查证价值，但不应继续作为活跃区入口

## 5. 归档后由哪些文档接管

后续优先阅读以下文档，而不是直接翻历史碎片：

- `gesture_project/docs/新对话对接_当前唯一有效入口_2026-06-12.md`
- `gesture_project/docs/项目全程阶段总结_阶段1_算法与数据集探索.md`
- `gesture_project/docs/项目全程阶段总结_阶段2_official回放与current_best收敛.md`
- `gesture_project/docs/项目全程阶段总结_阶段3_rowhandoff硬件参考线与真实trace闭环.md`
- `gesture_project/docs/项目全程阶段总结_阶段4_当前状态与后续执行路线.md`

## 6. 本次归档必须保留的细节

归档时不能丢：

- 训练配置
- 量化配置
- 回放命令
- `output_base` 规范
- 失败方向与失败原因
- 当前/旧 CSR 地址差异
- cocotb / runfiles / `/tmp` 写日志的坑
- rowhandoff trace 纠偏事实

这些内容都已经写入本轮新增阶段总结和对接入口文档中。

## 7. 执行要求

### 7.1 只迁移，不误删

任何材料都应迁移到备份目录，不做裸删。

### 7.2 先落文档，再迁移

先确保新总览、总 JSON、归档清单和新入口已经落地，再执行文件迁移。

### 7.3 后续查找方式

后续如果需要追溯旧试验：

1. 先看本清单判断它属于哪个阶段分桶。
2. 再去 `/home/steveguo/beifen/coralnpu-gesture` 的对应归档目录查。
